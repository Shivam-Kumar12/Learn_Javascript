const num=prompt("enter the number");

const result=`${num %2==0?"even":"odd" } numbers `
console.log(result);

const gender='F';

const usermessage=`${gender.toLocaleLowerCase()==='f'?"He":"She"} is a user`
console.log(usermessage);